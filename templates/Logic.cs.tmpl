using UnityEngine;
using UnityEngine.UIElements;

// YOUR HALF. Written once, then left alone — regenerating the UI never touches
// this file.
//
// Everything here is a draft for you to check and change. The generator knows
// what the UXML contains; it does not know what your game means. Treat the
// bodies below as a starting shape, not as working code.
//
// The fields and the event wiring live in the other half:
//   {{BINDINGS}}
// That file is rewritten on every run, so nothing you write should live there.
public partial class {{CLASS}}
{
    // Called at the end of OnEnable, once every element has been found.
    partial void OnBound()
    {
    }

{{HANDLERS}}}
